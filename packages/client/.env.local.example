# Client environment configuration

# Use server-based game (set to 'true' to use backend API, 'false' for local-only mode)
VITE_USE_SERVER=true

# Backend API URL (for server-based game mode)
VITE_API_URL=http://localhost:3000/api
