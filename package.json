{
  "name": "atoyr",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "yarn install",
    "dev": "concurrently \"yarn --cwd packages/client dev\" \"yarn --cwd packages/shared build && yarn --cwd packages/server dev\"",
    "build": "yarn --cwd packages/client build && yarn --cwd packages/server build",
    "test": "yarn workspaces foreach test"
  },
  "devDependencies": {
    "concurrently": "9.2.1",
    "prettier": "3.6.2",
    "prettier-plugin-organize-imports": "4.3.0",
    "typescript": "5.9.2"
  },
  "packageManager": "yarn@4.10.3"
}
