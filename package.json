{
  "name": "atoyr",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"yarn workspaces client dev\" \"yarn --cwd packages/server dev\"",
    "build": "yarn workspaces client build && yarn workspaces server build",
    "test": "yarn workspaces client test && yarn workspaces server test",
    "codegen": "yarn workspaces client codegen && yarn workspaces server codegen"
  },
  "devDependencies": {
    "concurrently": "9.2.1",
    "prettier": "3.6.2",
    "prettier-plugin-organize-imports": "4.3.0",
    "typescript": "5.9.2"
  },
  "packageManager": "yarn@4.10.3"
}
