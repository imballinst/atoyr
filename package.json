{
  "name": "atoyr",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "setup": "yarn install && go install github.com/air-verse/air@latest",
    "dev": "concurrently \"yarn workspace client dev\" \"yarn --cwd packages/server dev\"",
    "build": "yarn workspace client build && yarn workspace server build",
    "test": "yarn workspace client test && yarn workspace server test",
    "codegen": "yarn workspace client codegen && yarn workspace server codegen"
  },
  "devDependencies": {
    "concurrently": "9.2.1",
    "prettier": "3.6.2",
    "prettier-plugin-organize-imports": "4.3.0",
    "typescript": "5.9.2"
  },
  "packageManager": "yarn@4.10.3"
}